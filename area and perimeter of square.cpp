#include<stdio.h>
  main()
      {
         int side,area,perimeter;
		 printf("Enter a the value of side ");
		 scanf("%d",&side) 	;
		 area=side*side;
		 printf("area of square is %d\n",area);
		 perimeter=4*side;
		 printf("perimeter of square is %d",perimeter);
		 
      }
